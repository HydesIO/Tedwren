@{
    ViewData["Title"] = "Partners";
}
@await Html.PartialAsync("_Stub", new StubModel(
    "Partner with Tedwren",
    "An approval-gated application — never a site-access channel. Referral attribution, clawback and the partner dashboard are built in W7.",
    "W7"))
