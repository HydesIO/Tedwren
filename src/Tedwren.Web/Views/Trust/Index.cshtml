@{
    ViewData["Title"] = "Security & Trust";
}
@await Html.PartialAsync("_Stub", new StubModel(
    "Security & Trust",
    "A stable URL for procurement and DPOs. Only the claims we can make now — no fabricated badges, no absolute-compliance language.",
    "W5",
    CtaAction.BookDemo))
