@{
    ViewData["Title"] = "Book a demo";
}
@await Html.PartialAsync("_Stub", new StubModel(
    "Book a demo",
    "A short, validated form with calendar booking after submit — not \"we'll be in touch\". Built in W6.",
    "W6"))
