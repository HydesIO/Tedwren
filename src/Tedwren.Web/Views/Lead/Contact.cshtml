@{
    ViewData["Title"] = "Contact";
}
@await Html.PartialAsync("_Stub", new StubModel(
    "Get in touch",
    "A lightweight contact form routed by reason (General / Press / Partner / Support). Built in W6.",
    "W6"))
