@{
    ViewData["Title"] = "Pricing";
}
@await Html.PartialAsync("_Stub", new StubModel(
    "Simple, per-active pricing",
    "Subcontractor per-operative bands, main-contractor per-active-site, Worker Passport and pilot — every number from config, with the plain-language clarifiers.",
    "W4",
    CtaAction.BookDemo))
