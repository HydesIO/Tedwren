namespace Tedwren.Mobile.Controls.Resources;

/// <summary>
/// Typed resource dictionary holding the Tedwren colour tokens, merged into the app's resources so every
/// control and page resolves the same palette. Kept as a typed dictionary (x:Class) so it can be merged
/// cross-assembly from the app head.
/// </summary>
public partial class TedwrenColors : ResourceDictionary
{
    /// <summary>Loads the XAML-defined colour resources.</summary>
    public TedwrenColors() => InitializeComponent();
}
